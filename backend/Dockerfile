FROM python:3.11-slim

WORKDIR /app

# Instalar dependencias del sistema (útil para compilar librerías de machine learning/embeddings)
RUN apt-get update && apt-get install -y --no-install-recommends \
    build-essential \
    && rm -rf /var/lib/apt/lists/*

# Copiar dependencias primero para aprovechar la caché de Docker
COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

# Copiar el resto del código
COPY . .

EXPOSE 8000

# Asumiremos que el punto de entrada será src/main.py
CMD ["uvicorn", "src.main:app", "--host", "0.0.0.0", "--port", "8000"]
